const SCHEME_HOST = 'https://api.think-in.me';
const STAGE = 'dev';

const API_ENDPOINTS = {
  USERS: `${SCHEME_HOST}/${STAGE}/users`,
  TRANSLATION: `${SCHEME_HOST}/${STAGE}/translation`,
};

export { API_ENDPOINTS };
